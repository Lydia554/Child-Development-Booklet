setup:
  addons:
    - plan: heroku-postgresql

build:
  docker:
    web: ./backend/Dockerfile   # Backend Dockerfile
    frontend: ./frontend/Dockerfile   # Frontend Dockerfile

release:
  command:
    - echo "Deploying Multi-Container App"

run:
  web: node server.js
  frontend: npx serve -s dist -l 8080
